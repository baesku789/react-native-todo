import {ScrollView} from 'react-native';
import MakeTodoBtn from './MakeTodoBtn';

const TodoList = () => {
  return (
    <ScrollView>
      <MakeTodoBtn />
    </ScrollView>
  );
};

export default TodoList;
