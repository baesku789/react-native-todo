import {styled} from 'nativewind';
import {Pressable} from 'react-native';

const Button = styled(
  Pressable,
  'flex justify-center items-center bg-red1 h-40 rounded-full mx-auto',
);

export default Button;
